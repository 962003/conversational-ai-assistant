# Screenshots

Add screenshots here and reference them from the main README. Recommended shots:

1. **chat.png** — the chat interface answering a refund question (side-panel
   showing path + intent + confidence + sources + sentiment), with the
   **Direct / Dialogflow CX** toggle visible.
1b. **cx-path.png** — the same UI in **Dialogflow CX** mode (toggle green,
   "CX agent connected") answering via the live agent.
2. **dashboard.png** — the analytics dashboard (cards + top intents bars).
3. **dialogflow-flow.png** — the Dialogflow CX flow/intents in the console.
4. **webhook.png** — the CX webhook configuration pointing at `/dialogflow/webhook`.
5. **handoff.png** — the human handoff ticket modal / created ticket.
6. **swagger.png** — the FastAPI `/docs` page listing all endpoints.
